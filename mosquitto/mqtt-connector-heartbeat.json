{
  "name": "mqtt-heartbeat-source",
  "config": {
    "connector.class": "io.confluent.connect.mqtt.MqttSourceConnector",
    "tasks.max": "1",
    "mqtt.server.uri": "tcp://gridx-infra-mosquitto-1:1883",
    "mqtt.client.id": "gridx_kafka_heartbeat_bridge",
    "mqtt.qos": "1",
    "mqtt.topics": "gridx/+/+/heartbeat",
    "kafka.topic": "iot.heartbeats",
    "key.converter": "org.apache.kafka.connect.storage.StringConverter",
    "value.converter": "org.apache.kafka.connect.converters.ByteArrayConverter",
    "confluent.topic.bootstrap.servers": "gridx-infra-kafka-1:29092",
    "confluent.topic.replication.factor": "1"
  }
}